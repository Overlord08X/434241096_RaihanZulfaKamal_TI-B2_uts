import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF6200EE);
  static const Color primaryContainer = Color(0xFFEADDFF);
  static const Color onPrimary = Color(0xFFFFFFFF);

  // Secondary Colors
  static const Color secondary = Color(0xFF03DAC6);
  static const Color secondaryContainer = Color(0xFFB1F8F1);
  static const Color onSecondary = Color(0xFF000000);

  // Error Colors
  static const Color error = Color(0xFFCF6679);
  static const Color errorContainer = Color(0xFFF9DEDC);

  // Neutral Colors
  static const Color surface = Color(0xFFFFFBFE);
  static const Color surfaceDim = Color(0xFFECE6F0);
  static const Color onSurface = Color(0xFF1C1B1F);
  static const Color outline = Color(0xFF79747E);

  // Status Colors
  static const Color statusOpen = Color(0xFFFF6B6B);
  static const Color statusInProgress = Color(0xFFFFA500);
  static const Color statusDone = Color(0xFF51CF66);

  // Priority Colors
  static const Color priorityLow = Color(0xFF4CAF50);
  static const Color priorityMedium = Color(0xFFFFC107);
  static const Color priorityHigh = Color(0xFFFF5252);
}

class AppSpacing {
  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 16.0;
  static const double lg = 24.0;
  static const double xl = 32.0;
  static const double xxl = 48.0;
}

class AppRadius {
  static const double sm = 8.0;
  static const double md = 12.0;
  static const double lg = 16.0;
  static const double xl = 24.0;
  static const double full = 50.0;
}
